
<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Jaelle Wedsites", root_path, class: "navbar-brand" %>
    </div>
    <div class="navbar-collapse collapse pull-right">
      <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-link navbar-btn" %>
              <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-link navbar-btn" %>
              <% else %>
              <%= link_to "Login", new_user_session_path, class: "btn btn-link navbar-btn" %>
              <%= link_to "Sign up for a Free 14 day Trial", new_user_registration_path, class: "btn btn-primary navbar-btn" %>
           <% end %>
      </ul>    
    </div><!--/.navbar-collapse -->
  </div>
</div>